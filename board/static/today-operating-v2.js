/* Today canonical operating composition v4
   One composition owner, one data fetch, no MutationObservers or delayed rerenders. */
(() => {
'use strict';
if(!document.body.classList.contains('today-shell')||document.documentElement.classList.contains('wall-mode')||document.body.dataset.todayCanonical==='1')return;
document.body.dataset.todayCanonical='1';document.body.classList.add('today-operating-v4');
const d3=window.d3;if(!d3)return;
const picker=document.getElementById('dayPicker'),hero=document.querySelector('.hero'),heroMain=hero?.querySelector('.hero-main'),latest=hero?.querySelector('.latest'),context=document.querySelector('.context-strip'),eyebrow=document.getElementById('salesEyebrow');if(!picker||!hero||!heroMain||!latest||!context||!eyebrow)return;
const nf=new Intl.NumberFormat('en-US',{maximumFractionDigits:0}),money=v=>'$'+nf.format(Math.round(Number(v||0))),parseDate=s=>s?new Date(`${String(s).slice(0,10)}T12:00:00Z`):null,weekdayLetter=d=>['S','M','T','W','T','F','S'][d.getUTCDay()],pct=v=>v==null||!Number.isFinite(Number(v))?'—':`${Number(v)>=0?'+':'−'}${Math.abs(Number(v)).toFixed(0)}%`,weekdayName=s=>new Intl.DateTimeFormat('en-US',{weekday:'long',timeZone:'UTC'}).format(parseDate(s)),monthName=s=>new Intl.DateTimeFormat('en-US',{month:'long',timeZone:'UTC'}).format(parseDate(s));
const shortMoney=v=>{const n=Number(v||0),a=Math.abs(n);if(a>=1e6)return`${n<0?'−':''}$${(a/1e6).toFixed(a>=1e7?0:1)}m`;if(a>=1e3)return`${n<0?'−':''}$${(a/1e3).toFixed(a>=1e4?0:1)}k`;return`${n<0?'−':''}$${Math.round(a)}`};

/* Move existing renderer-owned nodes once. IDs remain intact, so the base renderer remains the only data owner. */
const stage=document.createElement('section'),main=document.createElement('div'),rail=document.createElement('aside');stage.className='today-operating-stage';main.className='today-operating-main';rail.className='today-state-rail';stage.setAttribute('aria-label','Current business state');rail.setAttribute('aria-label','Today operating context');hero.before(stage);stage.append(main,rail);main.appendChild(heroMain);rail.appendChild(latest);
const heroHead=document.createElement('div');heroHead.className='today-hero-head';heroMain.insertBefore(heroHead,heroMain.firstChild);heroHead.append(eyebrow,picker);
const pulse=document.createElement('section');pulse.className='today-pulse-read';pulse.innerHTML='<div class="today-pulse-kicker">Business pulse</div><strong id="pulseHeadline">Reading today…</strong><p id="pulseExplanation">Comparing the current day with recent operating context.</p>';rail.prepend(pulse);
const kpiGrid=document.createElement('div');kpiGrid.className='today-kpi-grid';[...context.children].forEach(n=>{n.classList.add('today-state-row');kpiGrid.appendChild(n)});pulse.after(kpiGrid);({weekLabel:'WTD',mtdLabel:'MTD',last30Label:'30D'});Object.entries({weekLabel:'WTD',mtdLabel:'MTD',last30Label:'30D'}).forEach(([id,text])=>{const e=document.getElementById(id);if(e)e.dataset.compactLabel=text});
const rhythmTitle=document.querySelector('.content-grid>.panel:first-child .panel-title');if(rhythmTitle)rhythmTitle.textContent='Recent rhythm';
const wins=document.querySelector('.wins');if(wins){wins.classList.add('today-orders-compact');const stream=wins.querySelector('#stream'),head=wins.querySelector('.panel-head');if(head)head.hidden=true;const details=document.createElement('details'),summary=document.createElement('summary');details.className='today-orders-details';summary.innerHTML='<span><strong>Orders</strong><small id="orderSummaryText">Transaction evidence for the selected day</small></span><b>View ↓</b>';if(stream)details.append(summary,stream);wins.appendChild(details);details.addEventListener('toggle',()=>{summary.querySelector('b').textContent=details.open?'Hide ↑':'View ↓'})}

/* Canonicalize immediately after the base renderer builds the picker. This happens in the same JS task, before paint: no observer, no second renderer, no label flicker. */
function canonicalizePicker(){const buttons=[...picker.querySelectorAll('.day-choice')];buttons.slice(0,8).forEach(btn=>{const d=parseDate(btn.dataset.date);if(!d)return;const live=btn.classList.contains('live'),b=btn.querySelector('b'),s=btn.querySelector('span');if(b)b.textContent=live?'Today':weekdayLetter(d);if(s)s.textContent=live?'':String(d.getUTCDate());btn.dataset.canonical='1';const long=new Intl.DateTimeFormat('en-US',{weekday:'long',month:'short',day:'numeric',timeZone:'UTC'}).format(d);btn.title=`${long}${live?' · live':''}`;btn.setAttribute('aria-label',btn.title)});buttons.slice(8).forEach(btn=>btn.remove());document.body.dataset.dayMode=picker.querySelector('.day-choice.active')?.classList.contains('live')?'live':'closed'}
function renderPulse(d){const t=d?.today||{},c=d?.context||{},live=!!d?.is_live,orders=Number(t.orders_today||0),pace=Number(t.pace_vs_same_weekday_pct),mtd=Number(c.mtd_delta_pct),week=Number(c.week_delta_pct),day=weekdayName(d.selected_date||d.local_today);let headline,explanation;if(live&&orders<3){headline=orders===0?'Still waiting for the day to declare itself':'Still early';explanation=orders===0?`No orders yet. ${monthName(d.selected_date||d.local_today)} MTD is ${pct(mtd)} vs the same days last month.`:`Only ${orders} order${orders===1?'':'s'} so far, so today’s pace is still low-signal. MTD is ${pct(mtd)}.`}else if(pace>=15){headline=`${live?'Ahead of':'A strong'} typical ${day} pace`;explanation=`${pct(pace)} vs comparable ${day.toLowerCase()} performance. Week is ${pct(week)} and MTD ${pct(mtd)}.`}else if(pace<=-15){headline=`${live?'Behind':'A soft'} typical ${day} pace`;explanation=`${pct(pace)} vs comparable ${day.toLowerCase()} performance. Week is ${pct(week)} and MTD ${pct(mtd)}.`}else{headline=`Tracking near a typical ${day}`;explanation=`${pct(pace)} vs comparable ${day.toLowerCase()} performance. Week is ${pct(week)} and MTD ${pct(mtd)}.`}document.getElementById('pulseHeadline').textContent=headline;document.getElementById('pulseExplanation').textContent=explanation;const os=document.getElementById('orderSummaryText');if(os)os.textContent=`${orders} order${orders===1?'':'s'} · ${Number(t.units_today||0)} units · ${money(t.sales_today||0)}`;const pt=document.getElementById('productsTitle'),ps=document.getElementById('productsSub'),ll=document.getElementById('latestLabel');if(pt)pt.textContent=live?'What is driving today':'What drove that day';if(ps)ps.textContent='Product mix · sales contribution';if(ll)ll.textContent='Latest sale'}
const baseRender=render;render=function canonicalRender(d){baseRender(d);canonicalizePicker();renderPulse(d)};

function rhythmRail(host,rows){const panel=host.closest('.panel');if(!panel)return;let out=panel.querySelector('.today-rhythm-kpi-rail');if(!out){out=document.createElement('div');out.className='today-rhythm-kpi-rail';panel.appendChild(out)}const sales=d3.sum(rows,r=>Number(r.sales||0)),orders=d3.sum(rows,r=>Number(r.orders||0)),units=d3.sum(rows,r=>Number(r.units||0)),pace=sales/Math.max(1,rows.length);out.innerHTML=`<div class="today-rhythm-kpi"><div class="label">Sales</div><strong>${money(sales)}</strong><small>selected window</small></div><div class="today-rhythm-kpi"><div class="label">Orders · units</div><strong>${orders} · ${units}</strong><small>volume</small></div><div class="today-rhythm-kpi"><div class="label">Daily pace</div><strong>${money(pace)}</strong><small>average / day</small></div><div class="today-rhythm-kpi"><div class="label">Range</div><strong>${rows.length}D</strong><small>${rows.length?d3.utcFormat('%b %-d')(parseDate(rows[0].business_date)):'—'} → ${rows.length?d3.utcFormat('%b %-d')(parseDate(rows.at(-1).business_date)):'—'}</small></div>`}
function drawRhythm(selector,inputRows,opts={}){const svg=d3.select(selector);if(svg.empty())return;const node=svg.node(),host=node.parentElement,rows=(inputRows||[]).map(r=>({...r,date:parseDate(r.business_date),sales:Number(r.sales||0),orders:Number(r.orders||0),units:Number(r.units||0)})).filter(r=>r.date);if(!rows.length){svg.selectAll('*').remove();return}const width=Math.max(300,Math.round(host.getBoundingClientRect().width)),compact=width<560,height=compact?190:width<900?184:178,m={top:12,right:8,bottom:30,left:compact?44:50},iw=width-m.left-m.right,ih=height-m.top-m.bottom;svg.selectAll('*').remove();svg.attr('viewBox',`0 0 ${width} ${height}`).attr('preserveAspectRatio','xMidYMid meet').classed('dpp-chart',true);const g=svg.append('g').attr('transform',`translate(${m.left},${m.top})`),x=d3.scaleBand().domain(rows.map(r=>r.business_date)).range([0,iw]).padding(rows.length<=8?.18:.24),y=d3.scaleLinear().domain([0,d3.max(rows,r=>r.sales)||1]).nice(4).range([ih,0]);g.append('g').attr('class','dpp-grid').call(d3.axisLeft(y).ticks(4).tickSize(-iw).tickFormat(''));g.append('g').attr('class','dpp-axis').call(d3.axisLeft(y).ticks(4).tickSize(0).tickPadding(7).tickFormat(shortMoney)).call(a=>a.select('.domain').remove());const selected=rows.at(-1)?.business_date,bars=g.selectAll('rect.today-rhythm-bar').data(rows).join('rect').attr('class','dpp-bar today-rhythm-bar').attr('x',r=>x(r.business_date)).attr('width',x.bandwidth()).attr('y',r=>y(r.sales)).attr('height',r=>Math.max(1,ih-y(r.sales))).attr('rx',Math.min(4,x.bandwidth()/4)).attr('fill',r=>{const dow=r.date.getUTCDay();if(opts.live&&r.business_date===selected)return'#e58b1f';return dow===0||dow===6?'#d8c09b':'#b78b4d'});const div=g.append('g').attr('pointer-events','none');rows.forEach((r,i)=>{if(!i)return;const prev=rows[i-1],xx=x(r.business_date)-(x.step()-x.bandwidth())/2;if(r.date.getUTCMonth()!==prev.date.getUTCMonth())div.append('line').attr('x1',xx).attr('x2',xx).attr('y1',0).attr('y2',ih).attr('stroke','#e58b1f').attr('stroke-width',1.4).attr('opacity',.82);else if(r.date.getUTCDay()===1)div.append('line').attr('x1',xx).attr('x2',xx).attr('y1',0).attr('y2',ih).attr('stroke','#c9c0b4').attr('stroke-width',.8).attr('opacity',.48)});const target=rows.length<=8?rows.length:width<520?4:width<850?5:7,step=Math.max(1,Math.ceil(rows.length/target)),ticks=rows.filter((r,i)=>rows.length<=8||i===0||i===rows.length-1||i%step===0).map(r=>r.business_date);g.append('g').attr('class','dpp-axis').attr('transform',`translate(0,${ih})`).call(d3.axisBottom(x).tickValues(ticks).tickSize(0).tickPadding(8).tickFormat(k=>d3.utcFormat(rows.length<=8?'%a':'%-d')(parseDate(k)))).call(a=>a.select('.domain').attr('stroke','#cfc5b7'));let tip=host.querySelector('.today-rhythm-tip');if(!tip){tip=document.createElement('div');tip.className='dpp-chart-tooltip today-rhythm-tip';host.appendChild(tip)}bars.attr('tabindex',0).on('pointerenter pointermove focus',function(_e,r){if(host.getBoundingClientRect().width<640)return;const hr=host.getBoundingClientRect(),br=this.getBoundingClientRect();tip.innerHTML=`<strong>${d3.utcFormat('%a, %b %-d')(r.date)}</strong><span>Sales ${money(r.sales)}</span><span>${r.orders} orders · ${r.units} units</span>`;tip.style.left=`${Math.min(hr.width-90,Math.max(90,br.left-hr.left+br.width/2))}px`;tip.style.top=`${Math.max(54,br.top-hr.top+8)}px`;tip.classList.add('show')}).on('pointerleave blur',()=>tip.classList.remove('show'));rhythmRail(host,rows)}
window.DPPCharts=window.DPPCharts||{};window.DPPCharts.dailyRhythm=drawRhythm;
const rhythm=document.getElementById('rhythm'),rhythmHost=rhythm?.parentElement;let lastWidth=0;if(rhythmHost&&window.ResizeObserver)new ResizeObserver(entries=>{const w=Math.round(entries[0]?.contentRect?.width||0);if(!w||Math.abs(w-lastWidth)<12||!DATA)return;lastWidth=w;drawRhythm('#rhythm',periodRows(),{live:!!DATA?.is_live})}).observe(rhythmHost);
})();
